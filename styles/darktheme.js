import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    text: {
      primary: "#FFFFFF",
    },
    background: {
      default: "#121212",
      paper: "#242424",
    },
    primary: { main: "#FFFFFF" },
  },
});

export default theme;
